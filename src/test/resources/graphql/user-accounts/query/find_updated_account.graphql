query {
    findAccount(username: "New Test Account") {
        username
        email
        address {
            country
            state
            city
            district
            street
            zip_code
        }
    }
}